{
  "name": "dsh-llm-gateway-local",
  "version": "0.1.0",
  "private": true,
  "description": "Model gateway for client applications: exposes an OpenAI/DeepSeek-compatible endpoint authenticated by a per-user token and forwards it to the deployment's real AI endpoint, so the client never holds an API key.",
  "type": "module",
  "main": "./lib/index.js",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
